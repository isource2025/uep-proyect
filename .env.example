# Required for local and Vercel
DATABASE_URL="sqlserver://<DB_HOST>:<DB_PORT>;database=<DB_NAME>;user=<DB_USER>;password=<DB_PASSWORD>;encrypt=false;trustServerCertificate=true"
BETTER_AUTH_SECRET="generate-with: openssl rand -hex 32"
BETTER_AUTH_URL="http://localhost:3000"
